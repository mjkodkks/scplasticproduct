---
import Link from "@components/ui/link.astro";
import { Image } from "astro:assets";
import AboutUsImage from "@assets/home/about_us.webp";
import Container from "@components/container.astro";
const wording1 = `S.C. PLASTIC PRODUCTS`
const wording2 = `เราคือผู้ผลิตและจำหน่ายพลาสติกมีเดียที่มีคุณภาพ ผลิตและจำหน่ายสินค้าประเภทตัวกลางพลาสติก 
ที่ใช้งานสำหรับในระบบบำบัดน้ำเสีย, ระบบบำบัดอากาศ, ระบบผลิตน้ำดี และ ดักไอน้ำ รวมทั้งรับขึ้นรูปพลาสติกบรรจุภัณฑ์/รับทำแม่พิมพ์ขึ้นรูปพลาสติกและให้คำปรึกษาสำหรับผลิตภัณฑ์ด้านสิ่งแวดล้อม ด้วยทีมงานที่มีประสบการณ์`
---

  <section class="text-gray-600 body-font bg-gradient-to-t from-white to-primary-300">
    <div class="container mx-auto flex px-5 py-24 md:flex-row flex-col items-center">
      <div class="lg:max-w-lg lg:w-full md:w-1/2 w-5/6 mb-10 md:mb-0">
        <Image src={AboutUsImage} alt={'About Us'} loading={"eager"} height={400} class="object-cover object-center rounded-2xl hero-image"></Image>
      </div>
      <div class="overflow-hidden lg:flex-grow md:w-1/2 py-8 px-12 flex flex-col md:items-start md:text-left items-center text-center bg-gray-400 rounded-md bg-clip-padding backdrop-filter backdrop-blur-sm bg-opacity-10">
        <h1 class="hero-wording hero-wording1 title-font sm:text-4xl text-2xl mb-4 font-medium text-gray-900">{wording1}
        </h1>
        <p class="hero-wording hero-wording2 leading-relaxed text-lg">{wording2}</p>
      </div>
    </div>
  </section>



<script>
  import gsap from "gsap";
  import ScrollTrigger from "gsap/ScrollTrigger";
  gsap.registerPlugin(ScrollTrigger);
  let t1 = gsap.timeline(
    {
      scrollTrigger: {
      trigger: ".hero-image",
      start: "-50% center",
      end: "200% center",
    },
    }
  )
  let t2 = gsap.timeline(
    {
      scrollTrigger: {
      trigger: ".hero-wording",
      start: "-50% center",
      end: "200% center",
    },
    }
  )

  t1.from(".hero-image", {y: 20, opacity: 0.5, duration: 0.3}).to(".hero-image", {y: 0, opacity: 1, duration: 0})
  t2.from(".hero-wording1", {x: 100, opacity: 0.5, duration: 0.3}).to(".hero-wording1", {x: 0, opacity: 1, duration: 0})
  t2.from(".hero-wording2", {x: 100, opacity: 0.5, duration: 0.3}).to(".hero-wording2", {x: 0, opacity: 1, duration: 0})
</script>